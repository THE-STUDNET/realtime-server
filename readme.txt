** INSTALL **

    - Install npm & node.js & redis
    - Install dependencies => npm install

** CONFIGURATION **

    - edit 'configuration.js'
    OR
    - create a file in 'envs' directory with custom configuration.

** Commands **

    - node app.js
    OR
    - node app.js --env "ENVS_CONFIG_FILE_NAME"
